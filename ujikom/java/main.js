<script>
  // Untuk mengaktifkan kembali elemen select
  document.getElementById("beasiswa").removeAttribute("disabled");
  
  // Untuk menonaktifkan kembali elemen select
  document.getElementById("beasiswa").setAttribute("disabled", "disabled");
</script>
